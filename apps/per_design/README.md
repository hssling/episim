# Per-design Hugging Face Spaces

This folder supports one Space per EPISIM design.

Set `EPISIM_DESIGN` in the Space environment to one registered design key, for example:

```text
EPISIM_DESIGN=regression_discontinuity
```

The wrapper then serves only that design with its default parameter schema.
